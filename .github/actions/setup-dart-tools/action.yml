name: "Setup Dart tools"
description: "Setup Dart SDK, install Melos and Mason, add pub-cache bin to PATH"
inputs:
  sdk:
    description: "Dart SDK version"
    required: false
    default: "stable"
runs:
  using: "composite"
  steps:
    - name: Setup Dart
      uses: dart-lang/setup-dart@v1
      with:
        sdk: ${{ inputs.sdk }}
        # setup-dart >=1.8 registers its matcher via GITHUB_ACTION_PATH, which
        # points at this composite action, so the matcher file isn't found and
        # the step fails.
        problem-matcher: "false"

    - name: Install Melos
      shell: bash
      run: dart pub global activate melos

    - name: Install Mason CLI
      shell: bash
      run: dart pub global activate mason_cli

    - name: Add pub-cache bin to PATH
      shell: bash
      run: echo "$HOME/.pub-cache/bin" >> $GITHUB_PATH

